- hosts: all
  gather_facts: true
  become: yes
  vars:
    git_list: []
  roles:
    - bases
    - users
    - git
    - docker
    - hostname
    - adauth
    - known_hosts
    - timezone
    - ssh
    - performance

